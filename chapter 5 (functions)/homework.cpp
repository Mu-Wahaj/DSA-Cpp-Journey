//  1 . write a function to check a nber is prime not?
//  2 . WAF  to print all prime nber from 1 to n?
//  3 . waF to print nth fabunachi?

#include <iostream>
using namespace std;

int checkPrime(int n)
{
    // int n;
    //  bool isPrime = true;
    //  for (int i = 0; i <=(n-1); i++)
    //  {
    //     if (n%i==0)
    //     {
    //         isPrime=false
    //         ;
    //         break;
    //     }
    //  else{
    //     cout<< "the is prime"<<endl;
    //  }
    //  }
     
    // return n;
}

int main() {
    // int num;
    // cout << "Enter a number: ";
    // cin >> num;

    // // Call the isPrime function and check the result
    // if (checkPrime(num)) {
    //     cout << num << " is a prime nber." << endl;
    // } else {
    //     cout << num << " is not a prime nber." << endl;
    // }

    // return 0;
       int n;
     bool isPrime = true;
     for (int i = 0; i <=(n-1); i++)

     {
        cout<<"hello";
        if (n%i==0)
        {
            isPrime=false
            ;
            break;
        }
     else{
        cout<< "the is prime"<<endl;
     }
     }
     
    return 0;
}


